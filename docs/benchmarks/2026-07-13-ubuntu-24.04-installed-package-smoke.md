# Ubuntu 24.04 installed-package observation (development artifact)

Date: 2026-07-13 (Pacific/Auckland)

This local observation ran the schema-15 `linux-x86_64-gnu` archive produced from the current
dirty development worktree inside a newly pulled stock Ubuntu 24.04 container. The container image
was `ubuntu:24.04` at digest
`sha256:4fbb8e6a8395de5a7550b33509421a2bafbc0aab6c06ba2cef9ebffbc7092d90`.
It contained no Rust toolchain or source build output. Only the archive, checksum manifest,
installer, and `scripts/installed-package-smoke.sh` were mounted read-only; ordinary runtime
prerequisites were installed with `apt`.

Result: `installed package smoke: ok` for Mealy 0.1.0, schema 15. The installed binaries passed
version/schema identity, health, status and doctor checks, completed one durable task, replayed it
with zero live provider/tool calls, reported exact settled usage, created and isolated-verified a
complete non-secret backup, drained cleanly, uninstalled, and preserved `mealy.sqlite3`.
The observation was repeated after the payload made `SECURITY.md` and `docs/THREAT_MODEL.md`
mandatory checksummed installed files and after the smoke required the checksum-matched
`install-mealy.sh` release asset instead of repository installer source; that corrected archive and
installer passed the same proof.

The same clean image subsequently inspected and installed the reproducible
`mealy_0.1.0_amd64.deb` constructed from that verified payload. `dpkg-deb` proved exact package
identity, root-owned 0755 binary targets, 0644 documents/manpages/changelog, fixed relative
command-link identity, `md5sums`, checksum-identical archive/Debian third-party license notices,
and a control archive containing no maintainer scripts. The
installed package generated the hardened owner unit, started schema 15, completed and
recorded-replayed a durable task, drained, and was removed while its temporary user database
remained. The then-current package also passed Ubuntu 24.04 Lintian 2.117 with no error or warning tags;
the remaining informational tags describe the deliberately embedded checksum-identical archive
documentation, unregistered standalone HTML documentation, ELF `.comment` sections, Rust fortify
heuristics, and binary spelling heuristics.
This validates the dirty-tree Debian package path, not a published repository or upgrade claim.

After the trusted runtime helper was made independent of ambient `PATH`, the replacement
non-final soak wrapper archive (`15af1d79607472ccebc50a0a4fee34a9ce12f7a34aec660cc17a66e5b818fd9d`)
was installed and exercised again. It completed schema-15 task
`019f5a8e-d41d-7f42-b4fd-c08651af6f09` with the sandbox required. A Debian package derived from
that exact wrapper had SHA-256
`57677fc96ee2ee7301e1f7d5f694fad556f6175e2ec0cb15c9b7ddfd09a84122`, declared
`libc-bin >= 2.39` for exact `/usr/bin/ldd` availability, and suggested the Ubuntu AppArmor profile
packages without adding maintainer scripts. Ubuntu 24.04 Lintian again emitted no error or warning,
and the clean-container Debian smoke completed task
`019f5aae-d574-73c0-b265-9d3a34fb13b2`. These hashes remain explicitly non-final because the
completed 24-hour report and updated documentation must be incorporated reproducibly afterward.
The container cannot exercise its host's AppArmor LSM; CI instead retains the namespace
restriction, activates Noble's packaged `bwrap-userns-restrict` profile, and requires the actual
private-network Bubblewrap probe before its sandbox suites.

## Generated systemd user-service integration

A later privileged Ubuntu 24.04 PID-1 container exercised the generated unit itself through a real
systemd user manager. This was intentionally deeper than the earlier package smoke, which checked
the unit text but ran the daemon directly. The first exact unit launch exposed root-owned system
files as overflow UID 65534 inside systemd's single-identity user namespace, so the exact trusted
`/usr/bin/ldd` check initially failed. The replacement trust check accepts that representation only
when the effective UID/GID maps are exact one-entry identity maps and the entire same-owner path
chain is non-writable and mounted read-only.

The next probes isolated `ProtectHostname=true`, `ProtectKernelLogs=true`, and
`ProtectKernelTunables=true` as incompatible with Bubblewrap's nested hostname and fresh `/proc`
setup. Replacing their process-view intent with `ProtectProc=invisible` and `ProcSubset=pid` made
both sandbox profiles enforceable. A real approved write then still failed with `path_denied`:
systemd's `RestrictSUIDSGID=true` seccomp policy blocks the worker's secure
`openat2(O_CREAT, ...)` call. Removing only that incompatible restriction while retaining
`NoNewPrivileges`, the private umask, Bubblewrap capability dropping, read-only system/home views,
and the other unit restrictions made the exact effect succeed.

The resulting checked `scripts/systemd-service-smoke.sh` repeated the complete generated-unit
lifecycle with exact optimized auditable binaries SHA-256
`d3d93166fd832b9515cc2ea29aebb9382b21e2bca4b8a8e391b0739979b10ff1` (`mealyd`) and
`5a8ab5f6b8c5108193921702bf5a2b879ab6f87d850664cd65606b1149509cdb` (`mealyctl`). A final repeat
installed those bytes from the candidate Debian package, then approved task
`019f5b29-8851-75f0-b6f6-db514ee45f0e` and required effect
`019f5b29-8909-7553-acb5-5ebc011b428e` to reach `succeeded`. It verified the exact file bytes and
drained/removed the temporary unit and home. The local container image ID was
`e0063c7e925ccf291fb0c243305a2a3d64afeca15c374f55350e0e0c3a7a0dd2`. This proves the current
dirty-tree x86_64 service integration, not the obsolete candidate package hashes above or the
still-required published native x86_64/ARM64 tag runs.

This is useful clean-runtime-host evidence, but it is deliberately not release evidence. The
archive came from an uncommitted dirty worktree, was not GitHub-attested or published, and covered
only x86_64. The release blocker still requires clean native x86_64 and ARM64 tag jobs and the
published-runbook check.

## 2026-07-14 exact corrected-candidate repeat

Ubuntu 24.04.4 repeated the generated-unit proof with the exact corrected package described in the
Debian observation: package SHA-256
`433ba9e6cfe94e88e021bfd7c2d7f128c14cc7d6f04b06d817a48ed8eec18291`, `mealyd` SHA-256
`408d5a45657b34e047122521e1d108fcc7e76ca51b237f96579d02c2869aa043`, and `mealyctl` SHA-256
`abdb04bb70bd7cfe04b86d6c921371f0418ea6c95b5a15ece150d547a54f7fa8`. A real lingering user
manager completed session `019f5b97-1ce3-75a3-9963-a619d0598688`, approved package-owned task
`019f5b97-1d07-7830-a507-ce014b40043f`, required effect
`019f5b97-1da9-7712-a26d-07cef3fa7f60` to succeed, verified the written bytes, drained, and left no
failed user unit. Reinstalling after disabling the stock minimal-image documentation exclusion made
`dpkg -V` clean and verified the complete documentation and manpage payload. This remains local
dirty-tree x86_64 evidence and does not replace the native published tag matrix.

The hardened harness was then repeated in a separate disposable Ubuntu 24.04 container running
systemd `255.4-1ubuntu8.16`. Container detection admitted the run without the physical-host opt-in;
the same `mealyd`/`mealyctl` hashes above completed task
`019f5bc3-b321-73c0-bb75-764371a57eb7` and effect
`019f5bc3-b3c8-7f62-9e13-6bd3d97ec9ed`. Cleanup left `mealy.service` inactive and undiscoverable,
no failed user unit, no matching daemon, and no temporary unit link. A normal Fedora host invocation
separately failed with exit 73 before contacting its user manager when
`MEALY_SYSTEMD_SMOKE_ALLOW_HOST` was absent. These checks validate the safer test lifecycle; they do
not add release provenance to the binaries.

## 2026-07-14 proxy-budget replacement candidate

The web-policy candidate above was superseded after a static browser audit found completed proxy
connection threads retained until whole-call shutdown. The replacement immediately reaps completed
handlers, holds concurrency through an unwind-safe lease, and stops each of the host/worker proxy
layers after 32 concurrent or 256 total connections. Focused 256-connection churn, full workspace,
and all three pinned-Chrome process suites passed.

Two cold auditable builds were byte-identical at `mealyd`
`bda5e8c4250612e6882711e70e15fa47e3f7661535160983dff906ffe1f4907e` and `mealyctl`
`82197a44ff30876c2e69a664d3cdc5a6cbc04a7684a8fd1909014e1c941428c0`. The normalized SBOM was
SHA-256 `df2a65c59348397ec4e9a314ebb416fd471521fd263e583a4f27ed4a7da87a1a`; the pre-evidence archive
was `fd3f963188959a9fb02da3b8ea7268cc1c92a98b210564d310f9eae3d13e26cc`, and its 18,678,040-byte
Debian package was `8981ce203a2b029883f792bd87fda5bbf56f568677ab28c8769d40857ba0d5c0`.

The archive smoke completed task `019f5bdd-c803-72f2-81f8-a403916aea0b`. A clean Ubuntu 24.04
direct package install/replay completed task `019f5bdf-57b7-7220-9d9c-851f10937b94`, and Lintian
2.117.0 emitted no fatal error or warning. A separate PID-1 container running systemd
`255.4-1ubuntu8.16` installed the exact package-owned binaries, then completed session
`019f5be0-edce-7c63-b3bb-349447ccff54`, approved task
`019f5be0-ede9-7640-956a-9ec7af1c7c36`, and required effect
`019f5be0-eea0-7872-abf8-b6bb4ee5449a` to succeed with exact file bytes. Cleanup left the unit
inactive/not-found, no failed user unit, and no daemon. The stock minimal-image documentation
exclusion again explains `dpkg -V` missing-doc output; the archive/direct package inspection had
already verified all checksummed documentation bytes. This remains dirty-tree, unattested x86_64
evidence and necessarily predates the final soak-inclusive package.
