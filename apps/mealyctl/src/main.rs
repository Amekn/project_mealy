//! Mealy's command-line client entry point.

fn main() {
    println!(
        "mealyctl {} architecture baseline; client implementation has not started",
        mealy_protocol::API_VERSION
    );
}
