# Schemas

Generated JSON schemas for API DTOs, event bodies, plugin manifests, config files, and memory records should live here.

Prefer deriving schemas from Rust types where practical so contracts stay aligned with implementation.
