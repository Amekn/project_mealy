# Migrations

Database migrations will live here once the SQLite event ledger and projection store are implemented.

The ledger schema should be treated as authoritative. Projection migrations must remain rebuildable from recorded events.
